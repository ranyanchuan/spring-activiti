<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yyan.dao.BookDao">

    <!--添加 book-->
    <insert id="insertBook" parameterType="com.yyan.pojo.Book">
        insert into book(title,page)
            values(#{title}, #{page})
    </insert>


</mapper>
